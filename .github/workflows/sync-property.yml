name: Sync Property Registries

on:
  workflow_dispatch:
    inputs:
      skip_preflight:
        description: 'Skip preflight checks (not recommended)'
        required: false
        type: boolean
        default: false
  schedule:
    # Run daily at 5 AM UTC
    - cron: '0 5 * * *'

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-property:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install notion-client pyyaml

      - name: Preflight checks
        if: ${{ !inputs.skip_preflight }}
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: |
          python scripts/preflight.py --bundle property

      - name: Dry-run export
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          CHITTY_ID: ${{ vars.CHITTY_ID || 'property-sync-workflow' }}
        run: |
          echo "Running dry-run to validate export..."
          python scripts/export_bundles.py property --dry-run

      - name: Export property bundle
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          CHITTY_ID: ${{ vars.CHITTY_ID || 'property-sync-workflow' }}
        run: |
          python scripts/export_bundles.py property

      - name: Verify bundle
        run: |
          python scripts/verify_bundle.py property

      - name: Check for changes
        id: git_status
        run: |
          git diff --exit-code packages/property/data/ || echo "has_changes=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.git_status.outputs.has_changes == 'true'
        run: |
          git config user.name "chittyos-bot"
          git config user.email "bot@users.noreply.github.com"
          git add packages/property/
          git commit -m "sync(property): automated export $(date +%Y-%m-%d)

          Bundle: property
          Exported by: ${{ github.actor }}
          ChittyID: ${{ vars.CHITTY_ID || 'property-sync-workflow' }}
          Workflow: ${{ github.workflow }}

          🤖 Generated with Claude Code"
          git push

      - name: Summary
        if: always()
        run: |
          echo "## Property Bundle Sync" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "**Bundle:** property" >> $GITHUB_STEP_SUMMARY
          echo "**ChittyID:** ${{ vars.CHITTY_ID || 'property-sync-workflow' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Changes:** ${{ steps.git_status.outputs.has_changes || 'none' }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f packages/property/manifest.json ]; then
            echo "### Manifest" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            cat packages/property/manifest.json >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
